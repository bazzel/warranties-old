= simple_form_for(@warranty, :html => { :class => 'form-horizontal' }) do |f|
  = f.input :name
  = f.input :warranty

  .form-actions
    = f.submit :class => 'btn-primary'
    = link_to "Cancel", warranties_path, :class => 'btn'
